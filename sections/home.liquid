<section class="hero-banners">
  <div class="banner-slider">
    <div class="banner-slide active" data-bg="mountain">
      <div class="banner-background">
        {% if section.settings.mountain_image %}
          <img src="{{ section.settings.mountain_image | image_url: width: 1920 }}" alt="Mountain snowboarder" class="banner-image" width="1920" height="1080">
        {% else %}
          <img src="https://static.vecteezy.com/system/resources/previews/046/454/450/large_2x/snowboarder-carving-action-close-up-snow-spray-vibrant-banner-background-ample-copy-space-photo.jpg" alt="Mountain snowboarder" class="banner-image" width="1920" height="1080">
        {% endif %}
        <div class="banner-overlay"></div>
      </div>
      <div class="banner-content">
        <div class="snow-particles"></div>
        <h1 class="banner-title">Conquer the Mountain</h1>
        <p class="banner-subtitle">Premium snowboards for every adventure</p>
        <a href="{{ section.settings.hero_button_link | default: routes.all_products_url }}" class="banner-button">{{ section.settings.hero_button_text | default: "Shop Snowboards" }}</a>
      </div>
    </div>
    
    <div class="banner-slide" data-bg="powder">
      <div class="banner-background">
        {% if section.settings.powder_image %}
          <img src="{{ section.settings.powder_image | image_url: width: 1920 }}" alt="Powder snowboarder" class="banner-image" width="1920" height="1080">
        {% else %}
          <img src="https://static.vecteezy.com/system/resources/previews/046/454/443/large_2x/snowboarder-carving-action-close-up-snow-spray-vibrant-banner-background-ample-copy-space-photo.jpg" alt="Powder snowboarder" class="banner-image" width="1920" height="1080">
        {% endif %}
        <div class="banner-overlay"></div>
      </div>
      <div class="banner-content">
        <div class="snow-particles"></div>
        <h1 class="banner-title">Fresh Powder Awaits</h1>
        <p class="banner-subtitle">Experience the thrill of untouched slopes</p>
        <a href="{{ section.settings.hero_button_link | default: routes.all_products_url }}" class="banner-button">{{ section.settings.hero_button_text | default: "Explore Gear" }}</a>
      </div>
    </div>
    
    <div class="banner-slide" data-bg="freestyle">
      <div class="banner-background">
        {% if section.settings.freestyle_image %}
          <img src="{{ section.settings.freestyle_image | image_url: width: 1920 }}" alt="Freestyle snowboarder" class="banner-image" width="1920" height="1080">
        {% else %}
          <img src="https://wallpaperaccess.com/full/16886.jpg" alt="Freestyle snowboarder" class="banner-image" width="1920" height="1080">
        {% endif %}
        <div class="banner-overlay"></div>
      </div>
      <div class="banner-content">
        <div class="snow-particles"></div>
        <h1 class="banner-title">Master Your Style</h1>
        <p class="banner-subtitle">Freestyle boards for park and pipe domination</p>
        <a href="{{ section.settings.hero_button_link | default: routes.all_products_url }}" class="banner-button">{{ section.settings.hero_button_text | default: "Shop Freestyle" }}</a>
      </div>
    </div>
  </div>
  
  <div class="banner-dots">
    <span class="dot active" data-slide="0"></span>
    <span class="dot" data-slide="1"></span>
    <span class="dot" data-slide="2"></span>
  </div>
</section>

<section class="featured-products">
  <div class="section-container">
    <div class="products-header">
      <h2 class="section-title">Featured Snowboards</h2>
      <div class="view-options">
        <span class="view-label">View:</span>
        <button class="view-btn " data-columns="3">3</button>
        <button class="view-btn" data-columns="4">4</button>
        <button class="view-btn active" data-columns="5">5</button>
      </div>
    </div>
    
    <div class="products-grid" data-columns="5">
      {% assign featured_collection = section.settings.featured_collection | default: collections.first %}
      {% assign products_limit = section.settings.products_to_show | default: 6 %}
      {% for product in featured_collection.products limit: products_limit %}
        <div class="product-card" style="animation-delay: {{ forloop.index | times: 0.1 }}s">
          <div class="product-badge">
            {% if product.compare_at_price > product.price %}
              <span class="sale-badge">Sale</span>
            {% endif %}
            {% if forloop.index <= 3 %}
              <span class="featured-badge">Featured</span>
            {% endif %}
          </div>
          
          <div class="product-image">
            {% if product.featured_image %}
              <img src="{{ product.featured_image | image_url: width: 400 }}" alt="{{ product.title }}" width="400" height="400">
            {% else %}
              <div class="product-placeholder">
                <div class="placeholder-content">
                  <span class="placeholder-icon">📦</span>
                  <span class="placeholder-text">Product Image</span>
                </div>
              </div>
            {% endif %}
            <div class="product-overlay">
              <a href="{{ product.url }}" class="quick-view">Quick View</a>
            </div>
          </div>
          
          <div class="product-info">
            <div class="product-category">Snowboard</div>
            <h3 class="text-xl font-normal">{{ product.title }}</h3>
            <div class="product-rating">
              <div class="stars">★★★★★</div>
              <span class="rating-count">({{ forloop.index | plus: 15 }})</span>
            </div>
            <div class="product-price">
              {% if product.compare_at_price > product.price %}
                <span class="sale-price">{{ product.price | money }}</span>
                <span class="original-price">{{ product.compare_at_price | money }}</span>
              {% else %}
                <span class="current-price">{{ product.price | money }}</span>
              {% endif %}
            </div>
          </div>
        </div>
      {% endfor %}
    </div>
  </div>
</section>

<section class="snowboard-features">
  <div class="section-container">
    <h2 class="section-title">Why Choose Our Boards</h2>
    <div class="features-grid">
      <div class="feature-card">
        <div class="p-6">
          <span class="icon-emoji">🏔️</span>
        </div>
        <h3 class="feature-title">Tested by Pros</h3>
        <p class="feature-description">Every board is tested by professional riders who push them to the limit on real mountains, ensuring you get equipment that performs when it matters most</p>
      </div>
      
      <div class="feature-card">
        <div class="p-6">
          <span class="icon-emoji">🎯</span>
        </div>
        <h3 class="feature-title">Perfect for Your Style</h3>
        <p class="feature-description">Whether you're carving groomers, hitting powder, or shredding the park, our team helps match you with the perfect board for your riding style</p>
      </div>
      
      <div class="feature-card">
        <div class="p-6">
          <span class="icon-emoji">🤝</span>
        </div>
        <h3 class="feature-title">Lifetime Support</h3>
        <p class="feature-description">Our experienced riders are here to help with setup, maintenance, and advice. We're not just selling boards, we're building a community of passionate snowboarders</p>
      </div>
      
      <div class="feature-card">
        <div class="p-6">
          <span class="icon-emoji">💯</span>
        </div>
        <h3 class="feature-title">Satisfaction Guaranteed</h3>
        <p class="feature-description">We stand behind every board we sell. If you're not completely satisfied with your purchase, we'll work with you to make it right</p>
      </div>
    </div>
  </div>
</section>

<section class="newsletter-section">
  <div class="section-container">
    <div class="newsletter-content">
      <h2 class="newsletter-title">Stay on the Cutting Edge</h2>
      <p class="newsletter-subtitle">Get the latest gear updates, mountain conditions, and exclusive offers</p>
      <form class="newsletter-form" action="{{ routes.account_url }}" method="post">
        <input type="email" name="contact[email]" placeholder="Enter your email" class="newsletter-input" required>
        <button type="submit" class="newsletter-button">Subscribe</button>
      </form>
      <p class="newsletter-privacy">We respect your privacy. Unsubscribe at any time.</p>
    </div>
  </div>
</section>

{% stylesheet %}
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

.hero-banners {
  position: relative;
  height: 60vh;
  overflow: hidden;
}

.banner-slider {
  position: relative;
  width: 100%;
  height: 100%;
}

.banner-slide {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  opacity: 0;
  transition: opacity 1s ease-in-out;
  display: flex;
  align-items: center;
  justify-content: center;
}

.banner-slide.active {
  opacity: 1;
}

.banner-background {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: 0;
}

.banner-image {
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: center;
  animation: imageZoom 20s ease-in-out infinite;
}

@keyframes imageZoom {
  0%, 100% { transform: scale(1); }
  50% { transform: scale(1.1); }
}

.banner-overlay {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: linear-gradient(
    45deg,
    rgba(0, 0, 0, 0.4) 0%,
    rgba(0, 0, 0, 0.2) 50%,
    rgba(0, 0, 0, 0.4) 100%
  );
  z-index: 1;
}

.banner-slide[data-bg="mountain"] .banner-background::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: linear-gradient(45deg, #1e3c72, #2a5298, #ffffff);
  background-size: 400% 400%;
  animation: mountainFlow 20s ease-in-out infinite;
  z-index: -1;
}

.banner-slide[data-bg="powder"] .banner-background::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: linear-gradient(135deg, #667eea, #764ba2, #f093fb);
  background-size: 400% 400%;
  animation: powderFlow 20s ease-in-out infinite;
  z-index: -1;
}

.banner-slide[data-bg="freestyle"] .banner-background::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, #ff6b6b, #4ecdc4, #45b7d1);
  background-size: 400% 400%;
  animation: freestyleFlow 20s ease-in-out infinite;
  z-index: -1;
}

.banner-slide[data-bg="mountain"] {
  background: linear-gradient(45deg, #1e3c72, #2a5298, #ffffff);
  background-size: 400% 400%;
  animation: mountainFlow 20s ease-in-out infinite;
}

.banner-slide[data-bg="powder"] {
  background: linear-gradient(135deg, #667eea, #764ba2, #f093fb);
  background-size: 400% 400%;
  animation: powderFlow 20s ease-in-out infinite;
}

.banner-slide[data-bg="freestyle"] {
  background: linear-gradient(90deg, #ff6b6b, #4ecdc4, #45b7d1);
  background-size: 400% 400%;
  animation: freestyleFlow 20s ease-in-out infinite;
}

@keyframes mountainFlow {
  0%, 100% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
}

@keyframes powderFlow {
  0%, 100% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
}

@keyframes freestyleFlow {
  0%, 100% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
}

.banner-content {
  text-align: center;
  color: white;
  z-index: 2;
  position: relative;
  animation: slideUp 1s ease-out;
}

@keyframes slideUp {
  from {
    transform: translateY(50px);
    opacity: 0;
  }
  to {
    transform: translateY(0);
    opacity: 1;
  }
}

.banner-title {
  font-size: 4rem;
  font-weight: 900;
  margin-bottom: 1rem;
  text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
  animation: titleGlow 3s ease-in-out infinite alternate;
}

@keyframes titleGlow {
  from { text-shadow: 2px 2px 4px rgba(0,0,0,0.3); }
  to { text-shadow: 2px 2px 20px rgba(255,255,255,0.5); }
}

.banner-subtitle {
  font-size: 1.5rem;
  margin-bottom: 2rem;
  opacity: 0.9;
  animation: fadeInDelay 1s ease-out 0.5s both;
}

@keyframes fadeInDelay {
  from { opacity: 0; transform: translateY(20px); }
  to { opacity: 0.9; transform: translateY(0); }
}

.banner-button {
  display: inline-block;
  background-color: #2563eb;
  color: white;
  padding: 1rem 2.5rem;
  text-decoration: none;
  border-radius: 50px;
  font-weight: 600;
  font-size: 1.1rem;
  transition: all 0.3s ease;
  box-shadow: 0 8px 25px rgba(0,0,0,0.2);
  animation: buttonBounce 2s ease-in-out infinite;
}

@keyframes buttonBounce {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-5px); }
}

.banner-button:hover {
  transform: translateY(-3px) scale(1.05);
  box-shadow: 0 12px 35px rgba(0,0,0,0.3);
}

.snow-particles {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  pointer-events: none;
  z-index: 1;
}

.snow-particles::before,
.snow-particles::after {
  content: '';
  position: absolute;
  top: -10px;
  left: 0;
  width: 100%;
  height: 110%;
  background-image: 
    radial-gradient(2px 2px at 20px 30px, white, transparent),
    radial-gradient(2px 2px at 40px 70px, white, transparent),
    radial-gradient(1px 1px at 90px 40px, white, transparent),
    radial-gradient(1px 1px at 130px 80px, white, transparent),
    radial-gradient(2px 2px at 160px 30px, white, transparent);
  background-repeat: repeat;
  background-size: 200px 100px;
  animation: snowfall 10s linear infinite;
}

.snow-particles::after {
  animation-delay: 5s;
  opacity: 0.5;
}

@keyframes snowfall {
  0% { transform: translateY(-100%); }
  100% { transform: translateY(100vh); }
}

.banner-dots {
  position: absolute;
  bottom: 2rem;
  left: 50%;
  transform: translateX(-50%);
  display: flex;
  gap: 1rem;
  z-index: 3;
}

.dot {
  width: 12px;
  height: 12px;
  border-radius: 50%;
  background: rgba(255,255,255,0.5);
  cursor: pointer;
  transition: all 0.3s ease;
}

.dot.active {
  background: white;
  transform: scale(1.2);
}

.dot:hover {
  background: rgba(255,255,255,0.8);
}

.featured-products {
  padding: 4rem 0;
  background: linear-gradient(180deg, #f8fafc 0%, #e2e8f0 100%);
}

.section-container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 2rem;
}

.products-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 3rem;
  flex-wrap: wrap;
  gap: 1rem;
}

.section-title {
  font-size: 2.5rem;
  color: #1e293b;
  font-weight: 700;
  position: relative;
  margin-bottom: 2rem;
}

.section-title::after {
  content: '';
  position: absolute;
  left: 50%;
  transform: translateX(-50%);
  top: 4.5rem;
  width: 50%;
  height: 4px;
  background: linear-gradient(45deg, #ff6b6b, #4ecdc4);
  border-radius: 2px;
}

.view-options {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  background: white;
  padding: 0.5rem;
  border-radius: 25px;
  box-shadow: 0 4px 15px rgba(0,0,0,0.1);
}

.view-label {
  font-weight: 600;
  color: #64748b;
  padding-top: 0.5rem;
  padding-bottom: 0.5rem;
  padding-left: 0.3rem;
  margin-right: 0.5rem;
}

.view-btn {
  background: transparent;
  border: none;
  padding: 0.5rem 1rem;
  border-radius: 20px;
  cursor: pointer;
  font-weight: 600;
  color: #64748b;
  transition: all 0.3s ease;
}

.view-btn.active,
.view-btn:hover {   
  background-color: #2563eb;
  color: white;
  transform: scale(1.05);
}

.products-grid {
  display: grid;
  gap: 2rem;
  transition: all 0.5s ease;
}

.products-grid[data-columns="3"] {
  grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
}

.products-grid[data-columns="4"] {
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
}

.products-grid[data-columns="5"] {
  grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
}

.product-card {
  background: white;
  border-radius: 15px;
  overflow: hidden;
  box-shadow: 0 4px 20px rgba(0,0,0,0.08);
  transition: all 0.4s ease;
  position: relative;
  animation: cardSlideUp 0.6s ease-out forwards;
  opacity: 0;
  transform: translateY(30px);
}

@keyframes cardSlideUp {
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.product-card:hover {
  transform: translateY(-8px);
  box-shadow: 0 12px 40px rgba(0,0,0,0.15);
}

.product-badge {
  position: absolute;
  top: 1rem;
  left: 1rem;
  z-index: 2;
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
}

.sale-badge,
.featured-badge {
  padding: 0.3rem 0.8rem;
  border-radius: 20px;
  font-size: 0.8rem;
  font-weight: 600;
  color: white;
}

.sale-badge {
  background: linear-gradient(45deg, #ff4757, #ff3838);
}

.featured-badge {
  background: linear-gradient(45deg, #ffa502, #ff6348);
}

.product-image {
  position: relative;
  overflow: hidden;
  height: 250px;
}

.product-image img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: transform 0.4s ease;
}

.product-card:hover .product-image img {
  transform: scale(1.1);
}

.product-placeholder {
  display: flex;
  align-items: center;
  justify-content: center;
  height: 100%;
  background: linear-gradient(135deg, #e2e8f0, #cbd5e1);
  flex-direction: column;
}

.placeholder-content {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 0.5rem;
}

.placeholder-icon {
  font-size: 2.5rem;
  opacity: 0.7;
}

.placeholder-text {
  font-size: 0.9rem;
  color: #64748b;
  font-weight: 500;
}

.product-overlay {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0,0,0,0.7);
  display: flex;
  align-items: center;
  justify-content: center;
  opacity: 0;
  transition: opacity 0.3s ease;
}

.product-card:hover .product-overlay {
  opacity: 1;
}

.quick-view {
  background: linear-gradient(45deg, #667eea, #764ba2);
  color: white;
  padding: 0.8rem 1.5rem;
  text-decoration: none;
  border-radius: 25px;
  font-weight: 600;
  transform: translateY(20px);
  transition: all 0.3s ease;
}

.product-card:hover .quick-view {
  transform: translateY(0);
}

.quick-view:hover {
  transform: scale(1.05);
  box-shadow: 0 8px 25px rgba(0,0,0,0.2);
}

.product-info {
  padding: 1.5rem;
}

.product-category {
  font-size: 0.9rem;
  color: #64748b;
  font-weight: 500;
  margin-bottom: 0.5rem;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.product-title {
  font-size: 1.25rem;
  font-weight: 700;
  color: #1e293b;
  margin-bottom: 0.8rem;
  line-height: 1.4;
}

.product-rating {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  margin-bottom: 1rem;
}

.stars {
  color: #fbbf24;
  font-size: 1.1rem;
}

.rating-count {
  color: #64748b;
  font-size: 0.9rem;
}

.product-price {
  display: flex;
  align-items: center;
  gap: 0.8rem;
}

.current-price,
.sale-price {
  font-size: 1.3rem;
  font-weight: 700;
  color: #1e293b;
}

.sale-price {
  color: #ef4444;
}

.original-price {
  font-size: 1.1rem;
  color: #64748b;
  text-decoration: line-through;
}

.hero-content {
  animation: fadeInUp 0.8s ease-out;
}

.hero-title {
  font-size: 3.5rem;
  font-weight: 700;
  margin-bottom: 1.5rem;
  line-height: 1.2;
}

.hero-subtitle {
  font-size: 1.25rem;
  margin-bottom: 2.5rem;
  opacity: 0.9;
  line-height: 1.6;
}

.hero-button {
  display: inline-block;
  background: white;
  color: #667eea;
  padding: 1rem 2.5rem;
  border-radius: 50px;
  text-decoration: none;
  font-weight: 600;
  font-size: 1.1rem;
  transition: all 0.3s ease;
  box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
}

.hero-button:hover {
  transform: translateY(-2px);
  box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15);
}

.hero-image {
  display: flex;
  justify-content: center;
  animation: fadeInRight 0.8s ease-out 0.3s both;
}

.hero-placeholder {
  background: rgba(255, 255, 255, 0.1);
  border-radius: 20px;
  padding: 2rem;
  backdrop-filter: blur(10px);
}

.section-container {
  max-width: 100%;
  margin: 0 auto;
  padding: 0 2rem;
}


.featured-products {
  padding: 6rem 0;
  background: #fafafa;
}

.products-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: 2rem;
}

.product-card {
  background: white;
  border-radius: 16px;
  overflow: hidden;
  box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
  transition: all 0.3s ease;
}

.product-card:hover {
  transform: translateY(-8px);
  box-shadow: 0 12px 40px rgba(0, 0, 0, 0.15);
}

.product-image {
  position: relative;
  height: 250px;
  overflow: hidden;
}

.product-image img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: transform 0.3s ease;
}

.product-card:hover .product-image img {
  transform: scale(1.05);
}

.product-placeholder {
  width: 100%;
  height: 100%;
  background: #f3f4f6;
  display: flex;
  align-items: center;
  justify-content: center;
}

.product-info {
  padding: 1.5rem;
}

.product-price {
  font-size: 1.1rem;
  font-weight: 700;
  color: #2563eb;
  margin-bottom: 1rem;
}

.product-link {
  display: inline-block;
  color: #2563eb;
  text-decoration: none;
  font-weight: 500;
  transition: color 0.3s ease;
}

.product-link:hover {
  color: #1d4ed8;
}

.snowboard-features {
  padding: 6rem 0;
  background: white;
  color: #1e293b;
}

.snowboard-features .section-title {
  text-align: center;
  color: #1e293b;
  margin-bottom: 4rem;
}

.snowboard-features .section-title::after {
  background: linear-gradient(45deg, #3b82f6, #06b6d4);
}

.features-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: 3rem;
}

.feature-card {
  text-align: center;
  padding: 2.5rem 2rem;
  background: #f8fafc;
  border-radius: 20px;
  border: 1px solid #e2e8f0;
  transition: all 0.4s ease;
  position: relative;
}

.feature-card:hover {
  transform: translateY(-10px);
  background: white;
  border-color: #3b82f6;
  box-shadow: 0 20px 50px rgba(59, 130, 246, 0.1);
}

.feature-icon {
  margin: 0 auto 1.5rem;
  width: 80px;
  height: 80px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: linear-gradient(45deg, #3b82f6, #06b6d4);
  border-radius: 50%;
  color: white;
  animation: iconFloat 3s ease-in-out infinite;
}

@keyframes iconFloat {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-10px); }
}

.icon-emoji {
  font-size: 2.5rem;
  color: white;
}

.feature-title {
  font-size: 1.4rem;
  font-weight: 700;
  margin-bottom: 1rem;
  color: #1e293b;
}

.feature-description {
  color: #64748b;
  line-height: 1.6;
  font-size: 1rem;
}

.feature-title {
  font-size: 1.5rem;
  font-weight: 600;
  margin-bottom: 1rem;
  color: #1f2937;
}

.feature-description {
  color: #6b7280;
  line-height: 1.6;
  font-size: 1rem;
}

.newsletter-section {
  padding: 6rem 0;
  background: linear-gradient(135deg, #1f2937 0%, #374151 100%);
  color: white;
}

.newsletter-content {
  text-align: center;
  max-width: 600px;
  margin: 0 auto;
}

.newsletter-title {
  font-size: 2.5rem;
  font-weight: 700;
  margin-bottom: 1rem;
}

.newsletter-subtitle {
  font-size: 1.1rem;
  margin-bottom: 2.5rem;
  opacity: 0.9;
}

.newsletter-form {
  display: flex;
  gap: 1rem;
  max-width: 400px;
  margin: 0 auto;
}

.newsletter-input {
  flex: 1;
  padding: 1rem 1.5rem;
  border: none;
  border-radius: 50px;
  font-size: 1rem;
  outline: none;
}

.newsletter-button {
  background: #2563eb;
  color: white;
  border: none;
  padding: 1rem 2rem;
  border-radius: 50px;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.3s ease;
}

.newsletter-button:hover {
  background: #1d4ed8;
  transform: translateY(-2px);
}

@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(30px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes fadeInRight {
  from {
    opacity: 0;
    transform: translateX(30px);
  }
  to {
    opacity: 1;
    transform: translateX(0);
  }
}

@media (max-width: 768px) {
  .hero-container {
    grid-template-columns: 1fr;
    text-align: center;
    gap: 2rem;
  }
  
  .hero-title {
    font-size: 2.5rem;
  }

  
  .newsletter-form {
    flex-direction: column;
  }
  
  .products-grid {
    grid-template-columns: 1fr;
  }
  
  .features-grid {
    grid-template-columns: 1fr;
    gap: 2rem;
  }
}

@media (max-width: 480px) {
  .hero-title {
    font-size: 2rem;
  }
  
  .section-container {
    padding: 0 1rem;
  }
  
  .hero-section {
    padding: 4rem 0;
  }
  
  .featured-products,
  .features-section,
  .newsletter-section {
    padding: 4rem 0;
  }
}
{% endstylesheet %}

{% schema %}
{
  "name": "Homepage",
  "settings": [
    {
      "type": "header",
      "content": "Banner Images"
    },
    {
      "type": "image_picker",
      "id": "mountain_image",
      "label": "Mountain Banner Image",
      "info": "Recommended: 1920x1080px. Shows epic mountain snowboarding action."
    },
    {
      "type": "image_picker",
      "id": "powder_image",
      "label": "Powder Banner Image",
      "info": "Recommended: 1920x1080px. Shows fresh powder snowboarding."
    },
    {
      "type": "image_picker",
      "id": "freestyle_image",
      "label": "Freestyle Banner Image",
      "info": "Recommended: 1920x1080px. Shows park and freestyle snowboarding."
    },
    {
      "type": "header",
      "content": "Banner Content"
    },
    {
      "type": "text",
      "id": "hero_title",
      "label": "Hero Title",
      "default": "Welcome to Our Store"
    },
    {
      "type": "textarea",
      "id": "hero_subtitle",
      "label": "Hero Subtitle",
      "default": "Discover amazing products crafted with care and passion"
    },
    {
      "type": "url",
      "id": "hero_button_link",
      "label": "Hero Button Link",
      "default": "/collections/all"
    },
    {
      "type": "text",
      "id": "hero_button_text",
      "label": "Hero Button Text",
      "default": "Shop Now"
    },
    {
      "type": "header",
      "content": "Featured Products"
    },
    {
      "type": "collection",
      "id": "featured_collection",
      "label": "Featured Collection"
    },
    {
      "type": "range",
      "id": "products_to_show",
      "label": "Products to Show",
      "min": 3,
      "max": 12,
      "step": 3,
      "default": 6
    }
  ]
}
{% endschema %}

{% javascript %}
document.addEventListener('DOMContentLoaded', function() {
  const bannerSlides = document.querySelectorAll('.banner-slide');
  const bannerDots = document.querySelectorAll('.banner-dots .dot');
  let currentSlide = 0;
  let bannerInterval;

  function showSlide(index) {
    bannerSlides.forEach((slide, i) => {
      slide.classList.toggle('active', i === index);
    });
    bannerDots.forEach((dot, i) => {
      dot.classList.toggle('active', i === index);
    });
  }

  function nextSlide() {
    currentSlide = (currentSlide + 1) % bannerSlides.length;
    showSlide(currentSlide);
  }

  function startBannerSlider() {
    bannerInterval = setInterval(nextSlide, 5000);
  }

  function stopBannerSlider() {
    if (bannerInterval) {
      clearInterval(bannerInterval);
    }
  }

  if (bannerSlides.length > 0) {
    startBannerSlider();
    
    bannerDots.forEach((dot, index) => {
      dot.addEventListener('click', () => {
        currentSlide = index;
        showSlide(currentSlide);
        stopBannerSlider();
        startBannerSlider();
      });
    });

    const bannerSlider = document.querySelector('.banner-slider');
    if (bannerSlider) {
      bannerSlider.addEventListener('mouseenter', stopBannerSlider);
      bannerSlider.addEventListener('mouseleave', startBannerSlider);
    }
  }

  const viewButtons = document.querySelectorAll('.view-btn');
  const productsGrid = document.querySelector('.products-grid');

  viewButtons.forEach(button => {
    button.addEventListener('click', () => {
      viewButtons.forEach(btn => btn.classList.remove('active'));
      
      button.classList.add('active');
      
      const columns = button.dataset.columns;
      if (productsGrid) {
        productsGrid.setAttribute('data-columns', columns);
      }
    });
  });

  const productCards = document.querySelectorAll('.product-card');
  
  const observerOptions = {
    threshold: 0.1,
    rootMargin: '0px 0px -50px 0px'
  };

  const productObserver = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
      if (entry.isIntersecting) {
        const card = entry.target;
        const index = Array.from(productCards).indexOf(card);
        
        setTimeout(() => {
          card.style.animationDelay = '0s';
          card.style.animation = 'cardSlideUp 0.6s ease-out forwards';
        }, index * 100);
        
        productObserver.unobserve(card);
      }
    });
  }, observerOptions);

  productCards.forEach(card => {
    card.style.opacity = '0';
    card.style.transform = 'translateY(30px)';
    productObserver.observe(card);
  });

  const featureCards = document.querySelectorAll('.feature-card');
  
  const featureObserver = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
      if (entry.isIntersecting) {
        const card = entry.target;
        const index = Array.from(featureCards).indexOf(card);
        
        setTimeout(() => {
          card.style.opacity = '1';
          card.style.transform = 'translateY(0)';
        }, index * 200);
        
        featureObserver.unobserve(card);
      }
    });
  }, observerOptions);

  featureCards.forEach(card => {
    card.style.opacity = '0';
    card.style.transform = 'translateY(50px)';
    card.style.transition = 'all 0.6s ease-out';
    featureObserver.observe(card);
  });

  function createSnowParticle() {
    const particle = document.createElement('div');
    particle.style.cssText = `
      position: absolute;
      width: ${Math.random() * 4 + 2}px;
      height: ${Math.random() * 4 + 2}px;
      background: white;
      border-radius: 50%;
      left: ${Math.random() * 100}%;
      top: -10px;
      animation: snowfall ${Math.random() * 5 + 5}s linear infinite;
      pointer-events: none;
      z-index: 1;
    `;
    return particle;
  }

  const snowContainers = document.querySelectorAll('.snow-particles');
  snowContainers.forEach(container => {
    for (let i = 0; i < 20; i++) {
      setTimeout(() => {
        const particle = createSnowParticle();
        container.appendChild(particle);
        
        setTimeout(() => {
          if (particle.parentNode) {
            particle.parentNode.removeChild(particle);
          }
        }, 10000);
      }, i * 200);
    }
  });

  const newsletterForm = document.querySelector('.newsletter-form');
  if (newsletterForm) {
    newsletterForm.addEventListener('submit', function(e) {
      const button = this.querySelector('.newsletter-button');
      const originalText = button.textContent;
      
      button.textContent = 'Subscribing...';
      button.style.background = 'linear-gradient(45deg, #10b981, #34d399)';
      
      setTimeout(() => {
        button.textContent = 'Subscribed!';
        setTimeout(() => {
          button.textContent = originalText;
          button.style.background = '';
        }, 2000);
      }, 1000);
    });
  }

  document.querySelectorAll('a[href^="#"]').forEach(anchor => {
    anchor.addEventListener('click', function (e) {
      e.preventDefault();
      const target = document.querySelector(this.getAttribute('href'));
      if (target) {
        target.scrollIntoView({
          behavior: 'smooth',
          block: 'start'
        });
      }
    });
  });
});
{% endjavascript %}